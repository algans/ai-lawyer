DATABASE_URL="postgresql://app:app@db:5432/hukuki"
ANTHROPIC_API_KEY="sk-ant-..."
SESSION_SECRET="en-az-32-karakter-rastgele-deger"
IYZICO_API_KEY="sandbox-..."
IYZICO_SECRET_KEY="sandbox-..."
IYZICO_BASE_URL="https://sandbox-api.iyzipay.com"
# Stripe (ikinci ödeme seçeneği, şimdilik test modu). Publishable key gerekmez.
# STRIPE_WEBHOOK_SECRET: `stripe listen --forward-to localhost:37429/api/payment/webhook` verir.
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
APP_URL="http://localhost:3000"
# Güvenilen ters proxy sayısı — rate-limit gerçek istemci IP'sini X-Forwarded-For'un
# sonundan bu kadar geriden alır. Yerelde/proxy yoksa 0.
# Fly.io: edge proxy tek hop ekler → 1 (deploy sonrası gerçek IP loglayıp doğrulayın).
GUVENILEN_PROXY_SAYISI="0"
